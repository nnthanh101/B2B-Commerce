# =============================================================
# Root-level environment overrides (optional)
# These values are picked up by docker-compose.yml when set.
# Copy to .env if you need to override defaults locally.
# Never commit .env (gitignored).
# =============================================================

# PostgreSQL — override only if port 5432 is already in use
# POSTGRES_PORT=5432
# POSTGRES_DB=medusa-store
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres

# Redis — override only if port 6379 is already in use
# REDIS_PORT=6379

# Node environment
# NODE_ENV=development

NEXT_PUBLIC_DEFAULT_REGION=nz

/**
 * Global teardown: any cleanup needed after tests complete.
 * Currently a no-op; available for future use (e.g., database cleanup).
 */
async function globalTeardown() {
  // No-op for P1
}

export default globalTeardown;
